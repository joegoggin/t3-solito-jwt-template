import { StyleSheet } from "app/utils/hooks/getClasses";

export const authFormStyles = {
    title: { main: "text-blue-700" },
    form: { main: "mt-10 w-[80%]" },
    btnContainer: { main: "mt-20" },
} satisfies StyleSheet;
